# squaresolvecalc
# 🚀 Интерактивный Калькулятор Квадратных Уравнений

Этот проект представляет собой консольное приложение на C++ для решения квадратных уравнений вида ax² + bx + c = 0. Программа обладает интерактивным интерфейсом, использует цветной вывод для улучшения читаемости и сохраняет историю всех вычислений.

▌✨ Особенности

•   Решение Квадратных Уравнений: Вычисляет действительные корни уравнений ax² + bx + c = 0.
•   Интерактивный Интерфейс: Позволяет вводить коэффициенты a, b, c в цикле и предлагает продолжить или выйти.
•   Цветной Вывод (ANSI): Использует ANSI escape-последовательности для выделения различных частей вывода (подсказки, ошибки, результаты) цветами.
•   Кроссплатформенность: Поддержка цветного вывода на Windows, Linux и macOS.
•   Корректное Форматирование Чисел: Специальная функция formatNumber обеспечивает чистый вывод чисел, убирая лишние нули и точки после целых чисел.
•   Обработка Случаев:
    •   Отрицательный дискриминант (нет действительных корней).
    •   Дискриминант равен нулю (один корень).
    •   Положительный дискриминант (два корня).
•   Валидация Ввода: Проверяет, является ли введенное значение числом.
•   Предупреждение a = 0: Если коэффициент a равен нулю, программа сообщает, что это не квадратное уравнение, и предлагает повторный ввод.
•   Подтверждение Уравнения: После ввода коэффициентов пользователь может подтвердить или отклонить сгенерированное уравнение, если оно выглядит некорректно.
•   История Вычислений: Все выполненные расчеты сохраняются в файл history.txt.
•   Русский Язык: Интерфейс полностью на русском языке благодаря setlocale(LC_ALL, "Russian").

▌🛠️ Сборка Проекта

Для сборки проекта вам потребуется компилятор C++ (например, g++, Clang или MSVC).

1.  Сохраните код: Сохраните предоставленный код в файл с расширением .cpp, например, quadratic_solver.cpp.

2.  Компиляция: Откройте терминал или командную строку и выполните следующую команду:

    
    g++ quadratic_solver.cpp -o quadratic_solver -std=c++11
    
    •   g++: Имя вашего C++ компилятора.
    •   quadratic_solver.cpp: Исходный файл.
    •   -o quadratic_solver: Указывает имя исполняемого файла (без расширения .exe для Linux/macOS, с расширением .exe для Windows).
    •   -std=c++11: Указывает использование стандарта C++11 или новее.

▌🚀 Запуск Программы

После успешной компиляции вы можете запустить программу:

•   Для Linux/macOS:
    
    ./quadratic_solver
    
•   Для Windows:
    
    quadratic_solver.exe
    

▌📝 Использование

1.  Запустите программу.
2.  Введите коэффициенты a, b, c по запросу, нажимая Enter после каждого.
3.  Подтвердите сформированное уравнение (введите y или да) или отклоните его (введите n), чтобы повторить ввод коэффициентов.
4.  Программа выведет дискриминант и найденные корни (если они есть).
5.  В конце каждого вычисления вам будет предложено решить ещё одно уравнение. Введите y (или да) для продолжения или n (или любой другой символ) для выхода.

▌💾 Файл history.txt

Все уравнения, которые были корректно введены и решены (даже если у них нет действительных корней), будут записаны в файл history.txt в корневой директории программы.

Пример содержимого history.txt:

```
1x^2+2x+1=0
D = 0; x = -1
1x^2-5x+6=0
D = 1; x1 = 2; x2 = 3
1x^2+1x+1=0
D = -3; корней нет.
```

---
